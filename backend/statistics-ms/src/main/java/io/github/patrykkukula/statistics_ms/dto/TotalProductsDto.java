package io.github.patrykkukula.statistics_ms.dto;

public record TotalProductsDto(Long totalProductsId,
                               Integer totalProducts,
                               Integer globalProducts,
                               Integer customProducts) {
}
